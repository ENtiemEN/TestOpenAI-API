Para instalar todas las dependencias necesarias, se crea un entorno virtual con Ej: `python -m venv venv`. Luego usan `pip install -r requirements.txt` (_Equivalente a un_ `npm install`)

El archivo `requirements.txt` fue creado mediante `pip freeze > requirements.txt`

Para activar tu entorno virtual en windows usas `.\venv\Scripts\activate`